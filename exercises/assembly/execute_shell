
# We first create the object file
echo "[+] Creating object file:"
nasm -f elf32 -o shell.o shell.asm
objdump -M intel -d shell.o

# Now we link and get the executable
echo "[+] Linking and creating executable:"
ld -m elf_i386 -o shell shell.o
echo Done
